# Project Instructions

- Run Black after every Python code edit:

  ```bash
  .venv/bin/python -m black src tests
  ```

- Run the test suite before handing work back:

  ```bash
  .venv/bin/python -m pytest
  ```
